<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:antcontrib="antlib:net.sf.antcontrib"
    name="umeng_community_sdk_ui"
    default="help" >

    <property
        name="work.dir"
        value="${script.dir}/output/temp" />

    <target name="repkg" >

        <copy todir="${work.dir}" >

            <fileset dir="src" >

                <include name="**/.java" />
            </fileset>

            <globmapper
                from="*.java"
                to="*.java.new" />

            <filterchain>

                <replacestring
                    from="gsl.dmex"
                    to="net.gslsrc.dmex" />
            </filterchain>
        </copy>

        <!-- 多此一举 -->

        <move
            overwrite="true"
            todir="${script.dir}/output/temp" >

            <fileset dir="${script.dir}/output/temp" >

                <include name="**/*.java.new" />
            </fileset>

            <globmapper
                from="*.java.new"
                to="*.java" />
        </move>
    </target>

</project>